name: Actualizar Torre de Despacho

on:
  schedule:
    # Cada 20 minutos. Nota: el scheduler de GitHub no es exacto,
    # en horas pico puede demorar. Los horarios son en UTC.
    - cron: "*/5 * * * *"
  workflow_dispatch:   # permite ejecutarlo manualmente desde la pestaña Actions

# Permisos necesarios para publicar en GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Evita que dos corridas se pisen
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Clonar el repositorio
        uses: actions/checkout@v4

      - name: Instalar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Generar el tablero (descarga CSV + procesa)
        run: python generar_tablero.py

      - name: Preparar carpeta de publicación
        run: |
          mkdir -p public
          cp index.html public/index.html

      - name: Subir artefacto de Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

      - name: Publicar en GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
